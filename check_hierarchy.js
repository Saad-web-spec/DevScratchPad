const fs = require('fs'); const content = fs.readFileSync('src/lib/blog/posts.ts', 'utf8'); const posts = content.split('slug:'); for(let i=1; i<posts.length; i++) { const m = posts[i].match(/##+ .+/g); if(m) { let lastLevel = 1; for(let j=0; j<m.length; j++) { const level = m[j].match(/#/g).length; if(level > lastLevel + 1) console.log('Skipped level in post ' + i + ': ' + m[j]); lastLevel = level; } } }
